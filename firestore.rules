rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regras mais permissivas para teste - ALTERAR EM PRODUÇÃO
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
